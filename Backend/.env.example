#############################################
# CareerGenie Backend Environment Variables #
#############################################

# Server
PORT=5000
NODE_ENV=development

# Firebase / Firestore (Auth, Storage)
FIREBASE_PROJECT_ID=carrergenie-55e58
FIREBASE_STORAGE_BUCKET=carrergenie-55e58.appspot.com

# Google Cloud / Vertex AI (Separate GCP project allowed)
VERTEX_AI_PROJECT_ID=annular-catfish-471811-h6
VERTEX_AI_LOCATION=us-central1

# (If still using API key auth — recommend migrating to ADC OAuth)
# VERTEX_AI_API_KEY=REPLACE_WITH_VERTEX_AI_API_KEY_OR_REMOVE_FOR_OAUTH

# Service Account Key path (relative to Backend/)
GOOGLE_APPLICATION_CREDENTIALS=./config/serviceAccountKey.json

# Feature / Safety Flags
REQUIRE_REAL_AI=true
ALLOW_AI_FALLBACKS=false
AI_RATE_WINDOW_MS=60000
AI_RATE_MAX=15
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security (if any custom JWT or encryption is implemented)
JWT_SECRET=REPLACE_WITH_STRONG_SECRET
# ENCRYPTION_KEY=32_BYTE_BASE64_STRING

# Frontend origin (CORS)
FRONTEND_URL=http://localhost:3000

# Logging / Observability (optional)
# SENTRY_DSN=

# Do NOT commit a real .env to source control
